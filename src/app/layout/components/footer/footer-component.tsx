import React from 'react';
import { FooterComponentProps } from './footer-component.props';

const FooterComponent = (props: FooterComponentProps) => {
    const { t } = props;
    console;
    return <></>;
};

export default FooterComponent;
